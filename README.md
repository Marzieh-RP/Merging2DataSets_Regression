# Merging2DataSets_Regression

In this project two datasets  are merged (Left join) together based on two common columns and then linear regression is applied to predict the "Boarding" dependent variables.
